(
  define [second a_list] 
  (define up (car a_list))
  (define ori (car (cdddr a_list)))
  (define newSec (+ ori up))

  (
    cond ([>= (/ newSec 60) 1] [
      (define upby (- (/ newSec 60) (modulo newSec 60)))
      (print upby)
    ])
  )
)

(
  define (time a_list)
  [second a_list]
)
