(
  define [second a_list] 
  (define up (car a_list))
  (define ori (car (cdddr a_list)))
  (define newSec (+ ori up))

(
    cond([> (div newSec 60) 0]
      (define minAdd (div newSec 60))
      (define finalSec (- newSec (* 60 minAdd)))
      (print finalSec)
    )
  )
)

(
  define (time a_list)
  [second a_list]
)
